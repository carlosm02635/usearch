# frozen_string_literal: true

RSpec.describe Usearch do
  it "has a version number" do
    expect(Usearch::VERSION).not_to be nil
  end

  it "does something useful", :skip do
    expect(false).to eq(true)
  end
end
